services:
  parser:
    image: ghcr.io/araticum/parser-monstro:latest
    ports:
      - "7000:7000"
    volumes:
      - ./storage:/app/storage
    environment:
      - ENABLE_EASYOCR=true
      - MAX_WORKERS=2
      - LOG_LEVEL=INFO
      - PARSER_MODE=precision_first
    restart: unless-stopped
